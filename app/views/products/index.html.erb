Item in cart: <div id="increment-cart"> </div>
<div id="cartCount"> items in cart: 0 </p>
    <% @products.each do |product| %>
<div class="row">
    <div class="col-sm-6 col-md-4">
        <div class="thumbnail">

            <%= image_tag(product.image, alt: "Image not available", size: "100x100") %>
            <br>
            <%= product.name %>
            ID:<%= product.id %>
            <br>
            <%= product.category %>
            <br>
            £<%= product.price %>
            <br>
            <%= product.quantity %> in stock
            <br>
        </div>
        <button type="button" id=<%=product.id%> > add to cart</button>
    </div>
</div>
<% end %>
